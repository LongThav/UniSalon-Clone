import React from 'react';
import { View, SafeAreaView, Text, } from 'react-native';

export const DetailShop = ()=>{
    return(
        <SafeAreaView style={{flex: 1, justifyContent: 'center'}}>
            <Text>Detail Shop View</Text>
        </SafeAreaView>
    );
}